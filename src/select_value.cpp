#include <select_value.hpp>

namespace iq
{
	/*
	 * I've so far been unsuccessful in getting this to compile... Not sure
	 * why. Works from the header file. >_>
	 */

	/*template<typename pair>
	const typename pair::second_type & select_value::operator()(const pair &p) const
	{
		return p.second;
	}*/
}

